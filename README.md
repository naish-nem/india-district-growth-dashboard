# India District Growth Dashboard

Interactive district-level dashboard for comparing nighttime lights, building footprint, and modeled building volume across India.

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The deployment build uses the prepared data committed in `public/data`. To regenerate the data locally from the source archive, run:

```bash
npm run prepare:data
```
